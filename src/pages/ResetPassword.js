import ResetPassword from "../features/auth/elements/ResetPassword";

function ResetPasswordPage() {
    return ( <div>
        <ResetPassword></ResetPassword>
    </div>  );
}

export default ResetPasswordPage;